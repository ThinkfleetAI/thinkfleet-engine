---
summary: "CLI reference for `thinkfleetbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `thinkfleetbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
thinkfleetbot uninstall
thinkfleetbot uninstall --all --yes
thinkfleetbot uninstall --dry-run
```

