---
summary: "CLI reference for `thinkfleetbot sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
---

# `thinkfleetbot sessions`

List stored conversation sessions.

```bash
thinkfleetbot sessions
thinkfleetbot sessions --active 120
thinkfleetbot sessions --json
```

